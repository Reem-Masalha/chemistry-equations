(()=>{
'use strict';
const $=id=>document.getElementById(id);
const SUB={'0':'₀','1':'₁','2':'₂','3':'₃','4':'₄','5':'₅','6':'₆','7':'₇','8':'₈','9':'₉'};
const REV={'₀':'0','₁':'1','₂':'2','₃':'3','₄':'4','₅':'5','₆':'6','₇':'7','₈':'8','₉':'9'};
const ui=(en,ar,he)=>{const l=localStorage.getItem('chemistryLanguage')||'en';return l==='ar'?ar:l==='he'?he:en};
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const norm=s=>String(s||'').replace(/[₀₁₂₃₄₅₆₇₈₉]/g,c=>REV[c]).replace(/\s+/g,' ').trim().replace(/=>|->|⟶|⇒|➜|⟹|⟾/g,'→');
function formula(f){let s=String(f||'').replace(/[₀₁₂₃₄₅₆₇₈₉]/g,c=>REV[c]),o='';for(let i=0;i<s.length;i++){if(/\d/.test(s[i])){let j=i;while(j<s.length&&/\d/.test(s[j]))j++;o+=s.slice(i,j).replace(/\d/g,d=>SUB[d]);i=j-1}else o+=s[i]}return esc(o)}
function molecule(p){const m=String(p||'').trim().match(/^(\d+)\s*(.*)$/);return m?`${esc(m[1])}${formula(m[2])}`:formula(p)}
function chem(eq){return norm(eq).split('→').map(side=>side.split('+').map(molecule).join(' + ')).join(' → ')}
const questions={
easy:[['H2 + O2 → H2O','2H2 + O2 → 2H2O','Hydrogen and oxygen must both be equal.'],['Na + Cl2 → NaCl','2Na + Cl2 → 2NaCl','Cl₂ has two chlorine atoms, so use 2 NaCl.'],['Mg + O2 → MgO','2Mg + O2 → 2MgO','O₂ contains two oxygen atoms.'],['N2 + H2 → NH3','N2 + 3H2 → 2NH3','Balance nitrogen first, then hydrogen.'],['Cl2 + H2 → HCl','H2 + Cl2 → 2HCl','Two chlorine atoms require two HCl molecules.'],['Fe + S → FeS','Fe + S → FeS','One Fe and one S already match.']],
medium:[['Fe + O2 → Fe2O3','4Fe + 3O2 → 2Fe2O3','Use six oxygen atoms on each side.'],['Ca + H2O → Ca(OH)2 + H2','Ca + 2H2O → Ca(OH)2 + H2','Two H₂O molecules provide two oxygen atoms.'],['Zn + HCl → ZnCl2 + H2','Zn + 2HCl → ZnCl2 + H2','ZnCl₂ requires two chlorine atoms.'],['CH4 + O2 → CO2 + H2O','CH4 + 2O2 → CO2 + 2H2O','Balance carbon, then hydrogen, then oxygen.'],['Na2O + H2O → NaOH','Na2O + H2O → 2NaOH','Two sodium atoms require two NaOH.'],['KClO3 → KCl + O2','2KClO3 → 2KCl + 3O2','Use six oxygen atoms on both sides.']],
hard:[['C3H8 + O2 → CO2 + H2O','C3H8 + 5O2 → 3CO2 + 4H2O','For combustion, balance C, then H, then O.'],['C2H5OH + O2 → CO2 + H2O','C2H5OH + 3O2 → 2CO2 + 3H2O','Balance carbon and hydrogen before oxygen.'],['NH3 + O2 → NO + H2O','4NH3 + 5O2 → 4NO + 6H2O','Balance nitrogen, hydrogen, then oxygen.'],['FeS2 + O2 → Fe2O3 + SO2','4FeS2 + 11O2 → 2Fe2O3 + 8SO2','Balance Fe, then S, then O.'],['C4H10 + O2 → CO2 + H2O','2C4H10 + 13O2 → 8CO2 + 10H2O','Balance C and H, then use oxygen to finish.'],['KMnO4 + HCl → KCl + MnCl2 + H2O + Cl2','2KMnO4 + 16HCl → 2KCl + 2MnCl2 + 8H2O + 5Cl2','Balance K and Mn first, then H, Cl, and O.']]};
const state={difficulty:'easy',experience:'practice',type:'choice',time:300,index:0,score:0,correct:0,answers:[],items:[],timer:null,running:false,answered:false};
function currentUser(){try{return JSON.parse(localStorage.getItem('chemistryCurrentUser')||'null')}catch{return null}}
function userKey(){const u=currentUser();return u?.id||u?.username||'guest'}
function statKey(){return 'chemistryQuizStats:'+userKey()}
function historyKey(){return 'chemistryQuizHistory:'+userKey()}
function readStats(){try{return JSON.parse(localStorage.getItem(statKey())||'{"sessions":0,"correct":0,"answered":0,"best":0}')}catch{return{sessions:0,correct:0,answered:0,best:0}}}
function readHistory(){try{const h=JSON.parse(localStorage.getItem(historyKey())||'[]');return Array.isArray(h)?h:[]}catch{return[]}}
function saveResult(){const answered=state.answers.length,correct=state.correct,pct=answered?Math.round(correct/answered*100):0;const s=readStats();s.sessions=(s.sessions||0)+1;s.correct=(s.correct||0)+correct;s.answered=(s.answered||0)+answered;s.best=Math.max(s.best||0,state.score);localStorage.setItem(statKey(),JSON.stringify(s));const h=readHistory();h.unshift({date:new Date().toISOString(),difficulty:state.difficulty,experience:state.experience,type:state.type,score:state.score,correct,answered,pct});localStorage.setItem(historyKey(),JSON.stringify(h.slice(0,20)));return{s,h}}
function coefficients(sol){return norm(sol).split('→').flatMap(side=>side.split('+').map(x=>x.trim()).filter(Boolean)).map(x=>{const m=x.match(/^(\d+)\s*/);return m?Number(m[1]):1})}
function formulaParts(eq){return norm(eq).split('→').flatMap(side=>side.split('+').map(x=>x.trim()).filter(Boolean))}
function withCoefficients(eq,cs){let i=0;return norm(eq).split('→').map(side=>side.split('+').map(p=>{const m=p.trim().match(/^(\d+)\s*(.+)$/),f=(m?m[2]:p).trim(),c=cs[i++]??1;return `${c===1?'':c}${f}`}).join(' + ')).join(' → ')}
function choices(solution){const c=coefficients(solution),out=[solution],vars=[c.map((n,i)=>Math.max(1,n+(i%2?1:-1))),c.map((n,i)=>Math.max(1,n+(i===0?1:0))),c.map((n,i)=>Math.max(1,n-(i%3===0?1:0)))];vars.forEach(v=>{const x=withCoefficients(solution,v);if(!out.includes(x))out.push(x)});while(out.length<4){const x=withCoefficients(solution,c.map((n,i)=>Math.max(1,n+(i%4)+1)));if(out.includes(x))break;out.push(x)}return out.sort(()=>Math.random()-.5).slice(0,4)}
function renderConfig(){document.querySelectorAll('[data-stage]').forEach(b=>b.classList.toggle('active-stage',b.dataset.stage===state.difficulty));const start=$('newQuiz');if(start)start.textContent=state.experience==='quiz'?ui('Start quiz','ابدأ الاختبار','התחל חידון'):ui('Start practice','ابدأ التدريب','התחל תרגול');document.querySelectorAll('input[name="experience"]').forEach(r=>r.checked=r.value===state.experience);document.querySelectorAll('input[name="qtype"]').forEach(r=>r.checked=r.value===state.type);const mw=document.querySelector('.mode-list');if(mw)mw.innerHTML=`<label><input type="radio" name="mode" value="timed" checked> ${ui('Timed · 5 minutes','مؤقت · 5 دقائق','מؤמד · 5 דקות')}</label>`}
function bindConfig(){document.querySelectorAll('[data-stage]').forEach(b=>b.addEventListener('click',()=>{if(!state.running){state.difficulty=b.dataset.stage;renderConfig()}}));document.querySelectorAll('input[name="experience"]').forEach(r=>r.addEventListener('change',()=>{if(!state.running){state.experience=r.value;renderConfig();updateHeader()}}));document.querySelectorAll('input[name="qtype"]').forEach(r=>r.addEventListener('change',()=>{if(!state.running){state.type=r.value;renderConfig()}}));const old=$('newQuiz');if(old){const start=old.cloneNode(true);old.replaceWith(start);start.addEventListener('click',startSession)}}
function updateHeader(){const t=$('quizTitle');if(t)t.textContent=state.experience==='quiz'?ui('Quiz','الاختبار','חידון'):ui('Practice','التدريب','תרגול');const p=$('timerText');if(p)p.textContent=state.experience==='quiz'?ui('Answer each question. Use Next question when you are ready.','أجب عن كل سؤال. استخدم السؤال التالي عندما تكون جاهزًا.','ענה על כל שאלה. השתמש בשאלה הבאה כשתהיה מוכן.'):ui('Get feedback after each answer and move on when you are ready.','احصل على ملاحظات بعد كل إجابة وانتقل عندما تكون جاهزًا.','קבל משוב לאחר כל תשובה והמשך כשתהיה מוכן.')}
function showCancel(show){const area=document.querySelector('.quiz-actions');if(!area)return;let b=$('cancelQuiz');if(show&&!b){b=document.createElement('button');b.id='cancelQuiz';b.type='button';b.className='secondary';b.textContent=ui('Cancel quiz','إلغاء الاختبار','ביטול חידון');area.prepend(b);b.addEventListener('click',cancelSession)}if(b)b.classList.toggle('hidden',!show)}
function startSession(){stopTimer();state.running=true;state.index=0;state.score=0;state.correct=0;state.answers=[];state.answered=false;state.items=[...questions[state.difficulty]].sort(()=>Math.random()-.5).slice(0,8);$('quizArea').innerHTML='';$('scoreArea').innerHTML='';$('retryMistakes')?.classList.add('hidden');updateHeader();renderHistory();renderQuestion();showCancel(true);if(state.experience==='quiz'){state.time=300;startTimer()}window.scrollTo({top:document.querySelector('.section.alt')?.offsetTop||0,behavior:'smooth'})}
function cancelSession(){stopTimer();state.running=false;state.items=[];state.answers=[];state.score=0;state.correct=0;state.index=0;$('quizArea').innerHTML=`<div class="quiz-empty-state">${ui('Quiz cancelled. Choose your settings and start again when you are ready.','تم إلغاء الاختبار. اختر الإعدادات وابدأ من جديد عندما تكون جاهزًا.','החידון בוטל. בחר את ההגדרות והתחל שוב כשתהיה מוכן.')}</div>`;$('scoreArea').innerHTML='';$('timer')?.classList.add('hidden');showCancel(false);renderConfig();renderHistory()}
function startTimer(){const t=$('timer');if(!t)return;t.classList.remove('hidden');drawTimer();state.timer=setInterval(()=>{state.time--;drawTimer();if(state.time<=0){stopTimer();finishSession(true)}},1000)}
function stopTimer(){if(state.timer){clearInterval(state.timer);state.timer=null}}
function drawTimer(){const m=Math.floor(state.time/60),s=String(state.time%60).padStart(2,'0'),t=$('timer');if(t)t.textContent=`⏱ ${m}:${s}`}
function renderStats(){const box=$('scoreArea');if(!box)return;const s=readStats();box.innerHTML=`<div class="practice-live-stats"><div><b>${state.score}</b><span>${ui('Score','النقاط','ניקוד')}</span></div><div><b>${state.correct}</b><span>${ui('Correct','صحيح','נכון')}</span></div><div><b>${state.running?state.index+1:0}</b><span>${ui('Question','السؤال','שאלה')}</span></div><div><b>${s.best||0}</b><span>${ui('Best score','أفضل نتيجة','שיא')}</span></div></div>`}
function renderHistory(){const s=readStats(),h=readHistory(),stats=$('quizStats'),list=$('historyList');if(stats)stats.innerHTML=`<div><b>${s.sessions||0}</b><span>${ui('Quizzes completed','اختبارات مكتملة','חידונים שהושלמו')}</span></div><div><b>${s.answered||0}</b><span>${ui('Questions answered','أسئلة تمت الإجابة عنها','שאלות שנענו')}</span></div><div><b>${s.correct||0}</b><span>${ui('Correct answers','إجابات صحيحة','תשובות נכונות')}</span></div><div><b>${s.best||0}</b><span>${ui('Best score','أفضل نتيجة','שיא')}</span></div>`;if(list){if(!h.length){list.classList.add('hidden');list.innerHTML='';}else{list.classList.remove('hidden');list.innerHTML=`<div class="history-title">${ui('Recent quiz history','سجل الاختبارات الأخير','היסטוריית החידונים האחרונה')}</div>`+h.map(x=>`<div class="history-row"><span>${new Date(x.date).toLocaleString()}</span><span>${x.experience==='quiz'?ui('Quiz','اختبار','חידון'):ui('Practice','تدريب','תרגול')} · ${x.difficulty}</span><strong>${x.score} · ${x.pct}%</strong></div>`).join('')}}}
function markAnswer(button,ok){if(button){button.classList.add(ok?'answer-correct':'answer-wrong');button.setAttribute('aria-pressed','true')}else{const box=document.querySelector('.coefficient-entry');if(box)box.classList.add(ok?'answer-correct':'answer-wrong');document.querySelectorAll('[data-coef]').forEach(i=>i.classList.add(ok?'answer-correct':'answer-wrong'))}}
function nextButton(){const f=$('answerFeedback');if(!f)return;const b=document.createElement('button');b.type='button';b.className='primary next-question';const last=state.index>=state.items.length-1;b.textContent=last?ui('See results','عرض النتائج','הצג תוצאות'):ui('Next question','السؤال التالي','השאלה הבאה');b.addEventListener('click',()=>{state.answered=false;state.index++;if(state.index<state.items.length)renderQuestion();else finishSession(false)});f.appendChild(b);f.classList.remove('hidden')}
function renderQuestion(){const item=state.items[state.index];if(!item)return finishSession(false);const [raw,solution,hint]=item,area=$('quizArea');if(!area)return;let controls='';if(state.type==='choice')controls=choices(solution).map((x,i)=>`<button type="button" class="practice-choice" data-answer="${encodeURIComponent(x)}">${String.fromCharCode(65+i)}. ${chem(x)}</button>`).join('');else controls=`<div class="coefficient-entry"><div class="coefficient-slots">${formulaParts(solution).map((_,i)=>`<input inputmode="numeric" pattern="[0-9]*" data-coef="${i}" placeholder="?" aria-label="Coefficient ${i+1}">`).join('')}</div><p class="muted">${ui('Enter coefficients from left to right. Include both sides and use 1 when needed.','أدخل المعاملات من اليسار إلى اليمين، للجانبين معًا، واستخدم 1 عند الحاجة.','הקלד את המקדמים משמאל לימין, לשני הצדדים, והשתמש ב-1 כשצריך.')}</p><button class="primary practice-submit" type="button">${ui('Submit answer','أرسل الإجابة','שלח תשובה')}</button></div>`;area.innerHTML=`<article class="practice-question-card"><div class="practice-question-meta"><span>${ui('Question','السؤال','שאלה')} ${state.index+1} / ${state.items.length}</span><span>${state.difficulty.toUpperCase()}</span></div><div class="practice-equation">${chem(raw)}</div><h3>${state.type==='choice'?ui('Choose the balanced equation','اختر المعادلة الموزونة','בחר את המשוואה המאוזנת'):ui('Type the coefficients','اكتب المعاملات','הקלד את המקדמים')}</h3><div class="practice-controls">${controls}</div><div id="answerFeedback" class="answer-feedback hidden"></div></article>`;area.querySelectorAll('.practice-choice').forEach(b=>b.addEventListener('click',()=>answerQuestion(decodeURIComponent(b.dataset.answer),solution,hint,b)));area.querySelector('.practice-submit')?.addEventListener('click',()=>{const vals=[...area.querySelectorAll('[data-coef]')].map(x=>Number(x.value||0)),wanted=coefficients(solution);answerQuestion(vals.map(String).join(','),wanted.map(String).join(','),hint,null,vals,wanted,solution)});renderStats()}
function answerQuestion(answer,solution,hint,button,typed,wanted,solutionText){if(!state.running||state.answered)return;state.answered=true;const ok=typed?typed.length===wanted.length&&typed.every((x,i)=>x===wanted[i]):norm(answer)===norm(solution);state.answers.push({ok});if(ok){state.correct++;state.score+=10}markAnswer(button,ok);document.querySelectorAll('.practice-choice').forEach(b=>b.disabled=true);const submit=$('quizArea')?.querySelector('.practice-submit');if(submit)submit.disabled=true;const f=$('answerFeedback');if(f){f.className=`answer-feedback ${ok?'feedback-correct':'feedback-wrong'}`;if(state.experience==='practice')f.innerHTML=ok?`<b>✓ ${ui('Correct','صحيحة','נכון')}</b><div>${ui('Great work!','أحسنت!','עבודה מצוינת!')}</div>`:`<b>✗ ${ui('Not quite','ليست صحيحة','לא בדיוק')}</b><div><strong>${ui('Correct answer:','الإجابة الصحيحة:','התשובה הנכונה:')}</strong> ${chem(solutionText||solution)}</div><div class="muted">${esc(hint||'')}</div>`;else f.innerHTML=ok?`<b>✓ ${ui('Correct','صحيحة','נכון')}</b>`:`<b>✗ ${ui('Incorrect','غير صحيحة','לא נכון')}</b>`;nextButton()}renderStats()}
function finishSession(timeUp){if(!state.running)return;stopTimer();state.running=false;const {s}=saveResult(),answered=state.answers.length,pct=answered?Math.round(state.correct/answered*100):0;showCancel(false);$('timer')?.classList.add('hidden');$('quizArea').innerHTML=`<article class="quiz-finish-card"><div class="finish-icon">${timeUp?'⏱':'✓'}</div><h2>${timeUp?ui('Time is up','انتهى الوقت','הזמן הסתיים'):ui('Quiz complete','اكتمل الاختبار','החידון הסתיים')}</h2><p>${ui('You answered','أجبت عن','ענית על')} <b>${answered}</b> ${ui('questions','أسئلة','שאלות')}.</p><div class="finish-score"><b>${state.score}</b><span>${ui('points','نقطة','נקודות')} · ${pct}% ${ui('correct','correct','נכון')}</span></div><div class="finish-actions"><button id="restartQuiz" class="primary" type="button">${ui('Try again','حاول مرة أخرى','נסה שוב')}</button><button id="backToSettings" class="secondary" type="button">${ui('Change settings','تغيير الإعدادات','שנה הגדרות')}</button></div></article>`;$('restartQuiz').onclick=startSession;$('backToSettings').onclick=()=>{$('quizArea').innerHTML=`<div class="quiz-empty-state">${ui('Choose your settings, then press Start.','اختر إعداداتك ثم اضغط ابدأ.','בחר את ההגדרות שלך ולחץ על התחל.')}</div>`;renderConfig()};$('scoreArea').innerHTML=`<div class="practice-live-stats"><div><b>${state.score}</b><span>${ui('Score','النقاط','ניקוד')}</span></div><div><b>${state.correct}</b><span>${ui('Correct','صحيح','נכון')}</span></div><div><b>${pct}%</b><span>${ui('Accuracy','الدقة','דיוק')}</span></div><div><b>${s.best||0}</b><span>${ui('Best score','أفضل نتيجة','שיא')}</span></div></div>`;renderHistory()}
function setup(){if(!$('quizArea'))return;renderConfig();bindConfig();updateHeader();showCancel(false);renderHistory();$('quizArea').innerHTML=`<div class="quiz-empty-state">${ui('Choose your settings, then press Start.','اختر إعداداتك ثم اضغط ابدأ.','בחר את ההגדרות שלך ולחץ על התחל.')}</div>`}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',setup);else setup();
})();