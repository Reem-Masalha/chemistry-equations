(()=>{
  'use strict';
  // Analytics is intentionally disabled on page startup while the site freeze is isolated.
  // Keeping this shim prevents missing-script errors without starting network work.
})();
