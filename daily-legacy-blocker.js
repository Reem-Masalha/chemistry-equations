(()=>{'use strict';if(!location.pathname.endsWith('learn.html'))return;const old=/Can you balance this\?|Try it in the Balancer|Start easy practice|Start today.?s challenge|One equation\. Every day\./i;const ids=['ce3-daily-card','daily-question-card','daily-home-challenge','dhc','dc5','spEntry','ce3-daily'];function clean(root=document){ids.forEach(id=>root.getElementById?.(id)?.remove());root.querySelectorAll?.('section,article,div').forEach(el=>{if(el.id==='daily-v5'||el.id==='daily-final'||el.id==='dc4'||el.id==='dc3'||el.id==='dc2')return;const t=(el.textContent||'').trim();if(t&&t.length<900&&old.test(t))el.remove()});document.querySelectorAll('.ce3-overlay').forEach(el=>el.remove())}function boot(){clean();const mo=new MutationObserver(()=>clean());mo.observe(document.body,{childList:true,subtree:true});}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();})();