(()=>{'use strict';if(!location.pathname.endsWith('learn.html'))return;const ids=['ce3-daily-card','daily-question-card','daily-home-challenge','dhc','dc5','spEntry','ce3-daily','daily-final','dc4','dc3','dc2','dailyChallenge'];function clean(root=document){ids.forEach(id=>root.getElementById?.(id)?.remove());root.querySelectorAll?.('.daily-card').forEach(card=>{if(!card.closest('#daily-v5'))card.closest('section,article,div')?.remove()});root.querySelectorAll?.('section,article,div').forEach(el=>{if(el.id==='daily-v5')return;const t=(el.textContent||'').trim();if(t&&t.length<900&&/DAILY CHEMISTRY CHALLENGE|TODAY.?S CHALLENGE|Can you balance this\?|Try it in the Balancer|Start easy practice|Start today.?s challenge|One equation\. Every day\./i.test(t))el.remove()});root.querySelectorAll?.('.ce3-overlay').forEach(el=>el.remove())}function boot(){clean();const mo=new MutationObserver(()=>clean());mo.observe(document.body,{childList:true,subtree:true});}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();})();
